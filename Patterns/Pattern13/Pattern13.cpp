#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;

    int row = 1;

    while (row <= n)
    {
        int col = 1;
        while (col <= n)
        {   
            //Here is two method for this problem;

            // char ch = (row + col - 2) + 'A';
            // char ch = (row + col - 1) + 64;
            
            // cout << ch;
            col = col + 1;
//         }
//         cout << endl;
//         row = row + 1;
    }
}
